{% extends 'base.html.twig'%}

{% block body %}
<div class="container">
<h1> Les derniers biens </h1>
{% for property in properties %}
<div class "col-4" style="display:inline-block; margin-right:40px;">

<div class="card">
<div class="card-body">

  <p><a href="{{path('property.show', {'id':property.id})}}"> {{ property.title}}</a></p>
  <p> {{ property.description}}</p>
  <p style="font-size:2em;font-style:bold;"> {{ property.price}} €</p>
 </div>
  </div>
</div>
{% endfor %}
</div>
{% endblock %}